git config --global user.name "username"
git config --global user.email "yourmail"
git list : shows history and info
git branch list all branches
git remote add name remote path
git remote -v
git stash
git stash pop
git rebase
git merge
